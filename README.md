# FocusMyTime

跨平台计时专注应用，支持 Windows、macOS、Linux、Android 和 Web。采用 Flutter 开发，多端体验一致。

## 核心功能

### 计时器系统
- **单核工作法**：自动计算到最近整点或半点，保证最少专注时长
- **番茄工作法**：专注 25min → 短休息 5min → 循环 → 长休息 15min（每 4 轮）
- 支持循环模式、自动开始下一轮
- **全方位通知系统**：专注或休息结束时触发铃声提醒、Windows 通知中心 Toast 以及应用内弹窗提醒

### 任务管理
- 多清单支持，自定义清单创建、编辑、删除
- **任务提醒事项**：类似 Microsoft To Do，支持设置指定日期的提醒，并支持 Windows 常驻闹钟模式。
- **全局时区设置**：支持跟随系统、北京、美国东部 3 种时区显示与时间输入，选项中直接展示 UTC 偏移。
- **版本更新检查**：启动后自动检查 GitHub Releases，也可在设置页底部手动检查最新版本。
- **跨清单拖拽**：长按任务拖拽到侧边栏其他清单，快速移动任务
- 重复任务：每天、每周、每月、每年循环
- 截止日期提醒，逾期任务红色高亮
- "我的一天"特殊视图

### 数据同步 (Cloud Sync)
- **多端同步协议**：基于 LWW (Last-Write-Wins) 的增量同步算法
- **软删除支持**：确保删除操作在所有设备间正确传播
- **自动触发**：任务变动及专注会话结束后自动同步至云端
- **反馈机制**：UI 实时显示同步状态及错误提示

## 技术架构

| 组件 | 技术选型 |
|------|----------|
| 框架 | Flutter (Client) / Node.js (Server) |
| 语言 | Dart / TypeScript |
| 状态管理 | Riverpod |
| 数据库 | SQLite（原生 sqflite / WebAssembly SQLite） |
| 服务端 | Express + Better-SQLite3 |
| 主题 | Material 3（本地中文字体回退） |
| 通知 | windows_notification & local_notifications |
| 音频 | audioplayers |
| 同步服务 | LWW 增量同步协议 |

### 🚀 最近更新 (v1.5.2)
- **跨端提醒与日历同步修复**：
    - 修复 Web 端新增提醒同步到 Android 后，系统日历可能出现两条同名事件的问题；提醒刷新会读取最新本机日历事件 ID，避免并发刷新用旧快照重复插入。
    - Android 日历桥接在旧 eventId 已不存在时会安全新建替代事件；普通任务更新误带 `calendarEventId` 时也不会推进同步时间戳。
- **桌面与 Web 交互修复**：
    - Windows 编辑任务标题后的“任务标题已保存”提示改为短时显示，并提供关闭按钮，保留撤销入口。
    - Web 端禁用浏览器原生右键菜单，右键任务或清单时只显示应用内操作菜单。
- **三端发布链路**：
    - GitHub Actions Release 增加 Web 构建压缩包，发布时会同时产出 Windows 安装包、Android APK 和 Web 静态资源包。

### 🚀 历史更新 (v1.5.1)
- **Web 稳定性与加载体验**：
    - 修复计时器暂停/恢复、后台节流和专注记录开始时间偏差，计时进度始终限制在有效范围。
    - 浏览器备份恢复改为先完整校验再覆盖数据，保留本机登录与 AI 密钥，并在恢复后刷新任务、提醒、日历与时区状态。
    - 修复日历切换日期后重复任务丢失，以及异步月份查询覆盖新页面状态的问题。
    - SQLite Web 增加单标签页写入保护；启动失败、重复打开和文件选择取消均提供明确提示。
    - 内置常用中文界面字形，首屏无需等待动态字体回退；CanvasKit 与完整回退字体均由站点本地提供，避免生产运行依赖外部 Google CDN。
- **同步与服务端防护**：
    - 客户端校验同步地址并提供更友好的网络错误；服务端增加严格请求结构校验、认证限流清理和可靠关停。
    - 服务端同步日志自动保留最近 90 天，且统计覆盖全部同步数据表。

### 🚀 历史更新 (v1.5.0)
- **Web 应用发布**：
    - 浏览器端使用 WebAssembly SQLite 保留本地数据能力，并提供 JSON 备份导入导出。
    - 同步服务通过 `https://focus.dluserver.cn` 提供 HTTPS API，浏览器端默认直连同域服务。
    - 系统日历和关闭页面后的系统提醒在 Web 端不可用；页面运行期间仍可使用应用内铃声提醒。
- **服务端安全加固**：
    - 生产环境强制配置 JWT 密钥，并限制跨域来源和认证接口请求频率。

### 🚀 历史更新 (v1.4.0)
- **重复任务与提醒可靠性修复**：
    - 修复 Android 勾选过期重复任务时界面卡住、连续点击会生成多个下一次任务的问题；重复任务完成与生成下一次任务现在以数据库事务原子执行，并增加点击进行中保护。
    - 重复任务支持更丰富的自定义规则：每天/每周/每月/每年、1-99 间隔、周几复选、每月按日期或星期、缺失日期回退/跳过，以及按日期或次数结束重复。
    - 设置提醒时取消额外底部弹出框，点击提醒行后直接进入日期和时间选择。
- **顶部任务清单自定义**：
    - 支持把自定义清单置顶到“我的一天、重要、任务”所在顶部区域，并可拖动排序、选择内置图标、取消置顶。
    - 支持隐藏系统预设清单，并在任务栏设置中一键恢复预设清单、重置清单图标和重置排序。
    - 清单置顶、图标、隐藏和排序配置随账号数据同步，其他客户端同步后可立即看到变更。
- **Android 与移动端体验优化**：
    - 修复 Android 平板横屏创建任务清单时无法取消输入框的问题；关闭输入法、点击输入框外或失焦都会取消创建，并保留输入草稿到应用退出。
    - Android 应用启动器名称改为 `FocusMyTime`，不更改包名，不影响覆盖更新。
    - 移动端任务详情页适配安全区与全宽布局，避免顶部关闭按钮贴近系统状态栏；底部“开始专注”区域增加安全间距。
    - 任务备注较长时点击备注会打开独立大文本框，便于查看和编辑长内容。
    - Android 下拉任务列表可触发同步，桌面端顶部工具栏新增手动同步按钮。
- **发布与更新体验修复**：
    - 修复 Android 16 点击更新弹窗“立即下载”后只关闭弹窗、不跳转浏览器的问题；现在会直接用外部浏览器打开 GitHub Release 下载页，打开失败时保留弹窗并提示原因。
    - 版本号升级至 `v1.4.0`，Windows 安装包版本、服务端版本和 GitHub Actions 手动发布默认版本同步更新。
- **日历与归档管理**：
    - Android 日历同步优先通过原生桥接按 `FocusMyTime` 本地账号和日历名复用日历，不存在才创建，避免重复堆积日历账号。
    - 新增自动归档设置：同一清单内同名已完成任务超过设定保留数量后自动归档，保留数量支持 0-9。
    - 归档管理改为按归档年月、日期分组展示归档清单和任务，右上角改为关闭按钮，恢复/删除后实时刷新内容。
- **Android 任务管理交互优化**：
    - 新建或重命名自定义清单时，清单较多也会自动滚动到输入框，避免 Android 软键盘遮挡正在输入的内容。
    - Android 端任务和自定义清单新增移动端“更多操作”入口，可通过触控完成归档、删除、重命名、移动等操作，不再依赖桌面右键。
    - 长按任务和清单继续用于拖拽排序或跨清单移动，避免归档/删除入口与拖拽手势冲突。

### 🚀 历史更新 (v1.2.0)
- **任务详情交互优化**：
    - 编辑任务名称时，按回车会确认保存并退出输入，不再插入换行。
    - 在新建任务、编辑任务名称和编辑任务备注的文本框中，Delete 键恢复为正常向后删除文本，不再被任务删除快捷键拦截。
    - 编辑提醒时间时，日期和时间选择器优先以任务当前设定的提醒时间为初始值；未设置提醒时再使用当前系统时间附近的默认值。
    - 任务详情的标题和备注输入撤销历史按任务隔离，避免 `Ctrl+Z` 跨任务恢复到之前选中过的任务内容。
    - 标题和备注保存后提供“撤销”操作，可从误覆盖或误保存中恢复到保存前内容。
- **同步登录可靠性修复**：
    - 同步 Token 失效时不再直接清空本地用户名和加密密码，会优先使用本地加密凭据自动重新登录并重试同步。
    - 自动重新登录失败时保留用户名和固定长度密码掩码，设置页可直接重新登录，不会暴露真实密码或真实密码长度。
    - 主界面和设置页增加同步登录失效警告，避免用户在未登录状态下误以为多设备仍在正常同步。
    - 本机同步凭据、服务端游标和 DeepSeek API Key 不再参与上传或下载覆盖，避免私密配置被跨设备污染。
- **同步容错与离线合并增强**：
    - 服务端新增权威变更游标，客户端本地待上传水位与远端拉取游标分离，降低设备时间不准导致漏拉增量的风险。
    - 服务端仍保留客户端编辑时间用于 LWW 冲突判断，避免离线旧编辑仅因较晚上线而覆盖较新的已同步编辑。
    - 任务同步新增字段级更新时间元数据，服务端和客户端会按任务字段合并不同设备的离线修改；例如一台设备改标题、另一台设备改备注时，不再互相整条覆盖。
    - 数据库版本升级到 11，新增 `sync_field_versions` 表并在升级时回填现有任务字段版本，用于后续字段级冲突判断。
    - 修复旧同步服务端数据库升级时可能在新增 `server_updated_at` 列之前创建索引，导致服务启动失败的问题，并补齐 `sync_log` 表初始化。
    - 同步失败会显示更具体的阶段和错误原因，不再只提示“同步失败或未配置”，便于排查 Android 提醒、网络、登录和服务端问题。

### 🚀 历史更新 (v1.1.2)
- **AI 任务助手修复**：
    - 修复 AI 创建任务时误判清单不存在、反复要求创建任务清单的问题。
    - AI 日期清单模式会优先复用已有清单 ID，重复创建清单操作不再阻断任务创建。
- **任务详情交互优化**：
    - 在任务详情中修改“预期时间（分钟）”时，支持按回车确认保存。
    - 预计时间输入限制为数字，并减少未变化内容的重复写入。

### 🚀 历史更新 (v1.1.1)
- **Android 体验与同步修复**：
    - Android 主界面适配沉浸式状态栏，移动端任务清单抽屉改为圆角边缘面板，展开时更自然。
    - 修复任务提醒刷新失败导致手动同步误报「同步失败或未配置」的问题；日历/通知后处理失败不再影响同步结果。
    - 日历事件 ID 改为本机私有更新，不再推进任务 `updated_at`，避免 Android 设置提醒后触发无意义同步冲突。
- **发布与更新修复**：
    - Android Release 改为使用稳定 release keystore secrets 签名，避免 CI 临时 debug 签名导致新 APK 无法覆盖安装。
    - 版本号升级至 `v1.1.1`，GitHub Actions 手动发布默认版本同步更新。
- **主题配色优化**：
    - 默认主题改为「星岩暮光」，使用靛紫、岩灰与琥珀强调色，减少绿色、青色系占比。

### 🚀 历史更新 (v1.1.0)
- **提醒与日历同步可靠性增强**：
    - Android 端打开 APP 或从后台回到前台时会自动执行后台同步，确保其它设备新增或修改的任务提醒能及时生效。
    - 任务、清单、设置与专注记录等变更改为防抖后台同步，频繁编辑时会合并请求，避免并发同步和重复刷新。
    - 统一“日历优先、系统通知兜底”的提醒路由：启用日历且权限正常时只同步系统日历，不再额外创建系统通知；日历不可用时自动回退通知。
    - Android 日历修改提醒时间继续使用 UPDATE，不走 DELETE+CREATE，适配 Android 16 对日历删除权限更严格的行为，避免旧日历事件残留造成重复提醒。
    - 日历事件 ID 改为本机字段，不再跨设备同步，避免 Windows/macOS/Android 互相覆盖本机日历事件引用。
- **任务管理增强**：
    - 新增归档任务与归档清单管理入口，支持查看、恢复和彻底删除归档内容。
    - 修复任务备注、提醒、到期时间、重要标记、预计时长等字段在多端同步中的覆盖问题。
    - Android 系统返回键支持关闭设置、AI、任务详情、日历与计时面板，最后再将 APP 退到后台。
- **界面与交互优化**：
    - 完成深海蓝绿主题体系，统一侧边栏、任务列表、任务详情、日历、AI 助手与计时器视觉风格。
    - 新增集中式图标与颜色 token，减少散落硬编码，提高后续主题维护性。
    - 优化计时器面板、任务输入、AI 操作预览、移动端布局与弹窗样式，提升桌面和 Android 的一致性。
- **发布准备**：
    - 版本号升级至 `v1.1.0`，GitHub Actions 手动发布默认版本同步更新。

### 🚀 历史更新 (v1.0.16)
- **绿色沉浸主题重构**：
    - 将原 Flutter 紫色主题重构为深海蓝绿视觉体系，侧边栏、顶部栏、任务列表、任务详情、日历、AI 助手与计时器控件统一使用新主题色。
    - 强化桌面端三栏布局的层次：侧边栏独立基底、任务选中态边框、添加任务输入框卡片化、专注计时面板增加独立背景与轻微阴影。
    - 保留现有任务选择、开始专注、番茄钟展开、任务详情、AI 操作审批、日历同步等功能逻辑，仅调整 UI 主题与展示质感。
- **主题维护性优化**：
    - 清理散落在组件中的紫色硬编码，统一通过 `AppColors` 管理强调色、成功色、警示色、边框和高层级面板色。
    - 新建日历默认颜色同步改为绿色主题色，避免外部系统日历继续使用旧紫色。

### 🚀 历史更新 (v1.0.15)
- **全局时区设置**：
    - 新增「设置 > 通用 > 时区」，内置跟随系统、北京（UTC+08:00）、美国东部（自动显示 UTC-05:00/UTC-04:00）三个选项。
    - 任务创建/修改时间、提醒时间、到期日判断、专注记录、日历统计、上次同步时间、更新检查日期等用户可见时间统一按所选时区展示。
    - AI 助手上下文、AI 操作预览与编辑中的时间解析同步使用当前时区，避免跨时区创建提醒或日期清单时出现偏差。
    - 切换时区后会刷新系统提醒与日历同步，确保外部日历事件与应用内时间语义一致。
- **检查更新入口**：
    - 设置页底部新增「检查更新」按钮，可手动查询 GitHub Releases 最新版本。
    - 手动检查会跳过每日自动检查限制，便于用户主动确认是否有新版安装包。

### 🚀 历史更新 (v1.0.14)
- **Windows 安装包发布修复**：
    - 修复 Windows 安装包启动时报 `sqlite3.dll` 缺失的问题，发布包会随应用携带 SQLite 原生库。
    - 修复 GitHub Actions Windows 构建长期 Queue 的问题，将 runner 固定到 `windows-2022`，避开已下线的 `windows-2019` 和 `windows-latest` 的 VS2026 兼容问题。
    - 优化 Release 流程，先上传构建产物再统一发布，确保 Android APK 与 Windows 安装包同时进入 GitHub Release。

### 🚀 历史更新 (v1.0.13)
- **任务重复功能完善 (仿 Microsoft To Do)**：
    - **自动生成下次任务**：当勾选完成带有重复属性的任务时，系统不再只是将其隐藏，而是会基于原任务自动生成下一个周期的全新任务实例，确保提醒和重复逻辑连贯。
    - **独立计算与解耦**：完成原任务时会剥离其重复配置（将其变为普通已完成任务），并将重复规则转移至新生成的任务，完美避免重复任务被意外多次生成的隐患。
    - **提醒与日历联动**：新生成的周期任务将自动根据新的到期日平移提醒时间，并同步推送到系统通知和系统日历。

### 🚀 历史更新 (v1.0.12)
- **Windows 提示音与专注时长修复**：
    - **Windows 系统默认铃声**：修复了专注完成后提示音只响一次的问题。通过强制设置通知场景为 `alarm` 并启用 `loop="true"`，实现了 Windows 系统默认闹钟铃声的循环播放。
    - **专注时长覆盖修复**：修复了在番茄模式下选择带有预期时长的任务时，时长被错误覆盖的问题。移除了非任务模式下的强制覆盖逻辑，确保各模式保持独立性。
    - **通知时长显示修复**：修复了任务模式下专注完成后，通知中显示“已专注 0秒”的 Bug。通过在状态重置前捕获时长，确保通知能正确显示实际专注时间。

### 🚀 历史更新 (v1.0.11)
- **计时器通知增强**：
    - 计时结束通知模板支持 `{duration}`（时长）和 `{mode}`（模式，单核/番茄）占位符。
    - 添加了时长格式化工具，支持显示“分秒”格式。
- **知识库与同步策略更新**：
    - **同步触发覆盖**：排查并记录了设置变更未触发同步的问题，提出了后台同步方案。
    - **AI 模型选择**：明确了任务管理场景下优先使用快速响应的 `deepseek-chat` 模型，而非耗时较长的推理模型。
    - **更新机制评估**：对比了 GitHub Releases 与自建更新服务器方案，推荐在核心功能稳定后自建更新服务器以保障隐私和体验。

### 🚀 历史更新 (v1.0.10)
- **日历同步 Android 16 兼容修复**：
    - 修复了修改任务提醒时间后系统日历出现重复日程的问题，将「先删后建」改为直接 UPDATE，跳过 Android 14+ 受限的 deleteEvent 操作。
    - 修复了取消提醒或删除任务后系统日历事件残留的问题，添加了 deleteEvent 失败时降级标记为 Canceled 的回退机制。
    - 增强了日历初始化日志，便于排查不同 Android 版本上的权限与归属问题。
- **状态同步优化**：`scheduleUnifiedReminders` 返回 eventId，确保任务状态与日历事件 ID 保持一致。

### 🚀 历史更新 (v1.0.9)
- **Android 稳定性与提醒优化**：
    - **修复白屏与时区崩溃**：解决了特定机型返回 `"TimezoneInfo"` 导致时区库崩溃的 Bug，增加了基于系统时间偏移的智能降级容错机制。
    - **彻底解决键盘闪退**：移除了 `build` 方法中误用的 `GlobalKey`，并优化了 Riverpod 细粒度监听，彻底解决了设置页面输入法自动收起的顽疾。
    - **界面适配状态栏**：为主框架引入了 `SafeArea`，完美解决了全面屏/刘海屏设备下顶部标题栏与系统状态栏重叠的问题。
    - **智能提醒路由（日历优先）**：实现了日历与本地通知的统一调度，有权限时优先同步至系统日历，无权限时回退至通知，防止双重打扰。

### 🚀 历史更新 (v1.0.8)
- **增强型任务提醒 (Microsoft To Do 风格)**：
    - 支持在任务详情页设置提醒时间，具备预设快捷选项。
    - **Windows 常驻闹铃**：使用 Windows Alarm 模式，提醒会持续显示并循环播放铃声，直至处理。
    - **交互式通知按钮**：提醒弹出后，可直接点击“开始专注”或“稍后提醒我（推迟）”。
    - **可配置推迟时长**：在设置中可自定义推迟提醒的分钟数。
- **稳定性修复**：
    - 修复了同步过程中右侧详情面板自动关闭的问题。
    - 优化后台同步逻辑，避免数据刷新时产生 UI 闪烁。

### 🚀 历史更新 (v1.0.7)
- **前后端源码合体**：将同步服务器 (Node.js/TypeScript) 源码集成到主仓库 `server/` 目录下，实现协议同步开发。
- **Android 端 UI 深度优化**：
    - 修复了移动端“任务列表”与“侧边栏”拖拽排序过于灵敏的问题，现统一使用**长按触发**。
    - 彻底解决 Android 端计时过程中的**全局刷新**与**像素溢出**警告，大幅提升流畅度。
- **核心逻辑修复**：修正了番茄钟循环模式下手动切换阶段导致的长休息判定错误。

### 🚀 早期更新 (v1.0.6)
- **自由拖拽排序 (Microsoft To Do 风格)**：
    - **任务手动排序**：支持在清单内自由拖拽任务调整优先级。
    - **清单手动排序**：侧边栏自定义清单支持拖拽排序。
- **增强型专注交互**：计时结束支持“继续专注”，底部状态栏按钮优化。
- **自动化云同步**：登录后自动同步，专注后自动触发后台数据刷新。

## 项目结构

```
FocusTimer (根目录)
├── lib/                        # Flutter 客户端源码
│   ├── main.dart               # 应用入口
│   ├── app.dart                # 主框架
│   └── features/               # 功能模块 (timer, tasks, calendar, etc.)
├── server/                     # 同步服务器源码 (Node.js/TypeScript)
│   ├── src/                    # 服务端核心逻辑 (auth, sync, db)
│   ├── package.json            # 服务端依赖配置
│   └── ecosystem.config.js     # PM2 部署配置
├── data/                       # 数据层相关
└── docs/                       # 项目文档与索引
```

详细文件索引请查看 [docs/project-index.md](docs/project-index.md)。

## 开发

```bash
# 安装依赖
flutter pub get

# 运行开发模式
flutter run

# 构建 Windows 版本
flutter build windows
```

## 文档与学习

- [KNOWLEDGE_BASE.md](KNOWLEDGE_BASE.md) — **开发者知识库**：记录了从 Electron 到 Flutter 的迁移经验、同步算法细节及解决方案。
- [功能模块索引](docs/project-index.md) — 代码文件快速定位
- [Web 构建与部署](docs/web-deployment.md) — 浏览器 SQLite、HTTPS 和反向代理说明

## 服务端部署

本项目自带一个用于多端同步的 Node.js 后端服务，位于 `server/` 目录下。

### 1. 安装依赖

```bash
cd server
npm install
```

### 2. 配置环境变量

复制环境配置文件示例并进行配置：

```bash
cp .env.example .env
```

在 `.env` 文件中配置以下内容：
- `PORT`: 服务运行端口（默认 6677）
- `HOST`: 服务监听地址（生产环境默认 `127.0.0.1`，仅由本机反向代理访问）
- `JWT_SECRET`: 用于签发 Token 的密钥，请在生产环境务必修改为一个随机的长字符串。
- `CORS_ORIGINS`: 允许访问同步 API 的 Web 来源，多个来源使用逗号分隔。

### 3. 运行服务

开发环境运行：
```bash
npm run dev
```

生产环境部署（推荐使用 PM2）：
```bash
npm run build
pm2 start ecosystem.config.js
```

## 版本

当前版本：v1.5.2 (Cross-platform Reminder Sync)
